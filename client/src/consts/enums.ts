const STATUS: { [key: number]: string } = {
  1: "created",
  2: "reviewing",
  3: "reviewed"
};

const PRIORITY: { [key: number]: string } = {
  1: "low",
  2: "medium",
  3: "high"
};

export { STATUS, PRIORITY };
